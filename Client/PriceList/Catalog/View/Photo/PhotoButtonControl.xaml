<UserControl x:Class="Catalog.View.Photo.PhotoButtonControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Catalog.View.Photo"
             mc:Ignorable="d" 
             d:DesignHeight="220" d:DesignWidth="140">
    <Grid>
       <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Button Grid.Row="1" Grid.Column="1" Margin="5" Background="{DynamicResource DefaultGradientBrush}">
            <Image Source="../../Picture/left.png" Height="30" Margin="3" />
        </Button>
        <Button Grid.Row="1" Grid.Column="2" Margin="5" Background="{DynamicResource DefaultGradientBrush}">
            <Image Source="../../Picture/right.png" Height="30" Margin="3" />
        </Button>
        <Button Grid.Row="2" Grid.Column="1" Grid.ColumnSpan="2" Margin="5" Background="{DynamicResource DefaultGradientBrush}">
            <Image x:Name="ScaleButtonImage" Source="../../Picture/collapce.png" Height="90" Margin="3" />
        </Button>
        <Button Grid.Row="3" Grid.Column="1" Grid.ColumnSpan="2"  Margin="5" Content="Закрыть" Background="{DynamicResource DefaultGradientBrush}" Foreground="White"></Button>
    </Grid>
</UserControl>
