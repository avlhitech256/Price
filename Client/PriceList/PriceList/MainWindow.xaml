<Window x:Class="PriceList.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PriceList"
        xmlns:presentation="clr-namespace:PriceList.Presentation"
        xmlns:footerStatusBar="clr-namespace:PriceList.Presentation.FooterStatusBar"
        mc:Ignorable="d"
        Title="Прайс лист «Авто Тренд»" Height="650" Width="1170" MinHeight="650"  MinWidth="1070" WindowState="Maximized">
    <DockPanel>
        <footerStatusBar:FooterStatusBarControl x:Name="FooterStatusBarControl" DockPanel.Dock="Bottom" />
        <Grid DockPanel.Dock="Top">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="Auto"></RowDefinition>
            </Grid.RowDefinitions>
            <presentation:HeaderControl x:Name="HeaderControl" Grid.Row="0"/>
            <presentation:TopMenuControl x:Name="TopMenuControl" Grid.Row="1" />
        </Grid>
        <UserControl x:Name="EntryControl" Content="{Binding Path=View}"/>
    </DockPanel>
</Window>
