<Window x:Class="PriceList.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PriceList"
        xmlns:presentation="clr-namespace:PriceList.Presentation"
        mc:Ignorable="d"
        Title="Прайс лист «Авто Тренд»" Height="350" Width="570" MinWidth="570">
    <DockPanel>
        <Grid DockPanel.Dock="Bottom">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="*"></RowDefinition>
            </Grid.RowDefinitions>
            <presentation:HeaderControl x:Name="HeaderControl" Grid.Row="0"/>
            <presentation:TopMenuControl x:Name="TopMenuControl" Grid.Row="1"></presentation:TopMenuControl>
        </Grid>
    </DockPanel>
</Window>
