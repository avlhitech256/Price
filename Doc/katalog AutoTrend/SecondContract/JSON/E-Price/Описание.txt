Логика расчета цены для контрагента:
Цену номенклатуры определяет тип цены, связь ними происходит по вхождению номенклатуры в группу (номенклатурная и ценовая).
У каждой номенклатуры указана ценовая и номенклатурная группы.
У контрагента в свою очередь установлены типы цен либо на номенклатурную группу либо на ценовую, 
если у контрагента нету типа цены не на одну из групп, то цена нулевая.
Номенклатурная группа приоритетней ценовой, следовательно если у контрагента есть тип цены по номенклатурной группе то она перекрывает цену по ценовой группе.

Список выгружаемых файлов:
1) PriceList.json (Файл содержит в себе информацию по номенклатуре)
2) Clients.json (Файл содержит в себе информацию по контрагентам)
3) MetaData.json (Файл содержит в себе информацию по стуктуре справочных данных)
4) каталог Photo (формат наименование файлов фотографий <внутренний код 1с>_F<номер фотографии, если отсутствует значит фотография основная>)


Структура файла PriceList.json

{
	"Nomenclature": [
		{
		"UID": "",			// Уникальный идентификатор	
		"Code": "",			// Внутренний код 1С 
		"VendorCode": "",		// Артикул
		"BrandUID": "",			// Уникальный идентификатор Бренда
		"Name": "",			// Наименование
		"Measure": "",			// Ед. измерения
		"NormPackaging": "",		// Норма упаковки
		"BatchOfSales": "",		// Партия продаж
		"PriceGroupUID": "",		// Уникальный идентификатор ценовой группы к которой принадлежит номенклатура
		"NomenclatureGroupUID": "",	// Уникальный идентификатор номенклатурной группы к которой принадлежит номенклатура
		"DateOfCreation": "",		// Дата создания
		"CatalogUID": "",		// Уникальный идентификатор каталога к которому принадлежит номенклатура
		"InStock": "",			// Остаток на складах	
		"TypesOfPrices": [		// Массив типов цен
			{
			"UID": "",		// Уникальный идентификатор типа цены
			"Price": ""		// Цена с валютой
			}
		],
		"CommodityDirection": [		// Массив товарных направлений
			""			// Уникальный идентификатор товарного направления
		],
		"Photos": [			// Массив Фотографий
			""			// Наименование файла с фотографией (без расширения)
		]
		}
	]
}


Структура файла Clients.json

{
	"Contragent": [
		{
		"UID": "",				// Уникальный идентификатор
		"Code": "",				// Внутренний код 1С
		"Name": "",				// Наименование
		"Login": "",				// Логин
		"MutualSettlements": "",		// Предоплата(отрицательное значение)\долг(положительное значение). 
		"PDZ": "",				// Просроченный долг клиента
		"PriceTypePriceGroup": [		// Массив типов цен по ценовым группам
			{
			"PriceGroupUID": "",		// Уникальный идентификатор ценовой группы
			"PriceTypeUID": ""		// Уникальный идентификатор типа цены
			}
		],
		"PriceTypeNomenclatureGroup": [		// Массив типов цен по номенклатурным группам
			{
			"NomenclatureGroupUID": "",	// Уникальный идентификатор номенклатурной группы
			"PriceTypeUID": ""		// Уникальный идентификатор типа цены
			}
		],
		"Discounts": [				// Массив скидок
			{
			"Rate": "",			// Процент скидки(отрицательное значение)\наценки(положительное значение). 
			"Nomenclature": [		// Массив номенклатры к которому отситься скидка
				""			// Уникальный идентификатор номенклатуры
			]
			}
		]
		}
	]
}


Структура файла MetaData.json

{
	"Catalog": [				// Массив дерева каталога
		{
		"UID": "",			// Уникальный идентификатор
		"Name": "",			// Наименование
		"Subdirectory": [		// Массив подкаталогов
			{
			"UID": "",		// Уникальный идентификатор
			"Name": "",		// Наименование
			"Subdirectory": [ 	// Массив подкаталогов
				... 		// Вложеность подкаталогов неограничена
			]	
			}
		]
		}
	],
	"PriceGroups": [			// Массив ценовых групп
		{
		"UID": "",			// Уникальный идентификатор
		"Name": ""			// Наименование
		}
	],
	"NomenclatureGroups": [			// Массив номенклатурных групп
		{
		"UID": "",			// Уникальный идентификатор
		"Name": ""			// Наименование
		}
	],
	"Brands": [				// Массив брендов
		{
		"UID": "",			// Уникальный идентификатор
		"Name": ""			// Наименование
		}
	],
	"TypesOfPrices": [			// Массив типов цен номенклатуры
		{
		"UID": "",			// Уникальный идентификатор
		"Name": ""			// Наименование
		}
	],
	"CommodityDirections": [		// Массив товарных направлений
		{
		"UID": "",			// Уникальный идентификатор
		"Name": ""			// Наименование 
		}
	]
}