<UserControl x:Class="WPFWait.LoadingControl.WaitControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WPFWait.LoadingControl"
             mc:Ignorable="d" 
             d:DesignHeight="80" d:DesignWidth="400">
    <Border Background="White" BorderBrush="#FF050027" BorderThickness="1,1,1,1">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="80"></ColumnDefinition>
                <ColumnDefinition Width="Auto"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <local:AtomLoadingControl Grid.Column="0"/>
            <Label x:Name="LoadingLabel" Grid.Column="1"
               Content="Подождите, идет загрузка данных..."
               VerticalContentAlignment="Center" Margin="12,0,20,0"/>
        </Grid>
    </Border>
</UserControl>
